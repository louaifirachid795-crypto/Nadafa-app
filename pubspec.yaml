name: nadafa_app
description: تطبيق تنبيه شاحنة النظافة
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:google_maps_flutter: ^2.13.1
    sdk: flutter
  geolocator: ^14.0.2

flutter:
  uses-material-design: true
